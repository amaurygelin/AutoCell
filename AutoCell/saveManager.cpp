#include "saveManager.h"

SaveManager::~SaveManager(){}

/*void AbstractDbManager::chargementAutomate()
{
    QSqlQuery query;
    query.exec("SELECT dimension FROM automaton_simple");
    query.first();
    int valeur = query.value(0).toInt();
    std::cout<<valeur<<"\n";
}
*/
